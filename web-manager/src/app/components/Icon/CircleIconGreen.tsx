'use client';

import { CircleIcon, IconProps } from "./CircleIcon";

export default function CircleIconGreen({ size }: IconProps) {
  return (
    <CircleIcon size={size} color="bg-green-600" />
  );
}