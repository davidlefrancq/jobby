'use client';

import { CircleIcon, IconProps } from "./CircleIcon";

export default function CircleIconBlue({ size }: IconProps) {
  return (
    <CircleIcon size={size} color="bg-blue-600" />
  );
}